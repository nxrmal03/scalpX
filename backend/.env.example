# ── Server ────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000

# ── Refresh interval (seconds) ────────────────────────
REFRESH_INTERVAL=10

# ── Optional premium API keys (fallbacks) ─────────────
# ALPHA_VANTAGE_API_KEY=your_key_here
# FINNHUB_API_KEY=your_key_here
